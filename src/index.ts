import { TModel, TSchema, ApptSequelize, getClass, getInstance } from './appt.sequelize';

export {
   ApptSequelize as Driver,
   getClass as Sequelize,
   getInstance as sequelize,
   TModel,
   TSchema
}
